{% extends 'base.html.twig' %}
{% block body %}
    <div class="page_content">
        {{ include('components/sidebar/sidebar.html.twig') }}
        <div class="product_content">
            <div class="product-review">
                <img class="product-image" src="{{ asset(imagePath) }}" alt="Symfony!"/>
                <div class="product-features"><h2>Характеристики</h2>{{ features }}</div>
            </div>
            <div class="product-description"><h2>Описание</h2>{{ description }}</div>
        </div>
        <div class="product-button">
            {{ component('Button', { message: 'Добавить' }) }}
        </div>
    </div>
{% endblock %}